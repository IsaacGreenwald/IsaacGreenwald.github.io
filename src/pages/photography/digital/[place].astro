---
import PhotoLayout from '../../../layouts/PhotoLayout.astro';
import JustifiedGallery from '../../../components/JustifiedGallery.jsx';
import ColorStrip from '../../../components/ColorStrip.astro';
import { getDigitalSets } from '../../../lib/photos.js';

export function getStaticPaths() {
  return getDigitalSets().map(set => ({
    params: { place: set.place },
    props: { set }
  }));
}

const { set } = Astro.props;
---

<PhotoLayout title={`${set.name} — Digital`} active="digital">
  <main class="page">
    <header class="head">
      <a class="back" href="/photography/digital">← Digital</a>
      <div class="title-row">
        <h1>{set.name}</h1>
        <div class="signature">
          <ColorStrip photos={set.photos} height={50} slice={3}/>
        </div>
      </div>
      <p class="meta">
        <span>{set.count} frames</span>
        {set.region && <span class="dot">·</span>}
        {set.region && <span>{set.region}</span>}
        {set.year && <span class="dot">·</span>}
        {set.year && <span>{set.year}</span>}
      </p>
    </header>

    <JustifiedGallery photos={set.photos} targetRowHeight={440} gap={16} rowGap={40} client:visible />
  </main>

  <style>
    .page {
      flex: 1;
      width: 100%;
      max-width: 1400px;
      margin: 0 auto;
      padding: 1rem 2.25rem 5rem;
    }
    .head {
      margin: clamp(0.5rem, 4vh, 2.5rem) 0 2.5rem;
    }
    .back {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      opacity: 0.5;
      transition: opacity 0.2s ease;
    }
    .back:hover { opacity: 1; }
    .title-row {
      display: flex;
      align-items: flex-end;
      gap: 1.5rem;
      flex-wrap: wrap;
      margin-top: 0.9rem;
    }
    .head h1 {
      font-family: var(--font-serif);
      font-weight: 500;
      font-size: clamp(2rem, 4.5vw, 3.25rem);
      line-height: 1.05;
    }
    .signature {
      flex: 1 1 200px;
      max-width: 360px;
      margin-bottom: 0.55rem;
    }
    .sig-label {
      display: block;
      margin-top: 0.4rem;
      font-size: 0.6rem;
      text-transform: uppercase;
      letter-spacing: 0.2em;
      color: #6a6a6a;
    }
    .meta {
      margin-top: 0.6rem;
      display: flex;
      gap: 0.5rem;
      font-size: 0.72rem;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: #8a8a8a;
    }
    .meta .dot { opacity: 0.5; }

    @media (max-width: 540px) {
      .page { padding: 1rem 1.25rem 4rem; }
    }
  </style>
</PhotoLayout>
